# Backend API base URL (no trailing slash). Default: http://localhost:3002
VITE_API_URL=http://localhost:3002

# Optional: WebSocket URL. Defaults to same as VITE_API_URL.
# VITE_WS_URL=http://localhost:3002
